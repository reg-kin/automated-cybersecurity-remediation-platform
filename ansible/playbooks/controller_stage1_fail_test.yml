---
- name: Stage 1 failure test
  hosts: all
  gather_facts: false
  become: false

  tasks:
    - name: Controlled remediation step
      ansible.builtin.debug:
        msg: "Controlled Stage 1 failure test"

    - name: Deliberately fail Stage 1 verification
      ansible.builtin.assert:
        that:
          - false
        fail_msg: "Intentional Stage 1 verification failure for Test 6"
